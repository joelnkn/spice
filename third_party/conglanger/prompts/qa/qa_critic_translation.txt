Here is a translation result for a constructed language:
=== TRANSLATION START ===
{input_sentences}
=== TRANSLATION END ===

For context, here is the full language specification:
=== SPEC START ===
{context}
=== SPEC END ===

Here are detected conflicts between newly proposed words and the existing lexicon:
=== LEXICON CONFLICTS START ===
{lexicon_conflicts}
=== LEXICON CONFLICTS END ===

Your task: Evaluate the accuracy and consistency of the translation.

Distinguish among:
- base lexemes (listed in lexicon)
- inflected forms (base + inflectional affixes)
- derived forms (base + derivational affixes)
- newly coined base lexemes (true new words)
Derived/inflected forms MUST NOT be treated as new words.

Return JSON with:

1. "overall_score" (1–10) based on:
   orthography, affix legality, feature-vector compliance,
   lexicon usage, morphological correctness, gloss accuracy,
   semantic accuracy.

2. "issues": list of:
   {{
     "issue": "<short description>",
     "type": "<orthography_violation|affix_error|feature_violation|lexical_error|glossing_error|semantic_error|consistency_error>",
     "correction": "<how to fix>",
     "priority": <1|2|3>
   }}


SPECIAL CONSISTENCY RULES:

1. **Missing new_words entries:**
   - If the translation uses a word that:
       • is NOT in the lexicon,  
       • is NOT analyzable as base + valid affixes, and  
       • is NOT in the “new_words” list,  
     report a **priority 1** `"consistency_error"`.

2. **Incorrect affix use:**
   - Only affixes in the inventory may be used.
   - Must be used with correct function and order.
   - Affixes cannot be treated as new words.

3. **Derived forms:**
   - Allowed only if the derivation (POS→POS) is legal.
   - If illegal → "affix_error"
   - DO NOT treat them as new words.

4. **Lexicon conflicts:**
   - For each conflict in LEXICON CONFLICTS:
       • Add a **priority 1 "consistency_error"**.  
       • The correction must correction must rename or adjust POS/translation.

5. **Necessity of new words:**
   For each invented base lexeme:
   - If the meaning could have been expressed using a different existing word or paraphrase, add a **priority 1 "lexical_error"**.
   - If the word resembles English or violates orthography, report a priority 1 "orthography_violation".

6. **Gloss correctness:**
   - Glosses must match affix boundaries and true functions.
   - Glossing mistakes are `"glossing_error"`.

Output rules:
- PRIORITY MUST be 1, 2, or 3 ONLY. Never 0 or "none".
- If something is not an issue, do NOT include it.
- If total issues > 20 → list only priority 1–2 (drop all priority 3).
- Output ONLY the JSON.
